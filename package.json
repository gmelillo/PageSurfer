{
  "name": "PageSurfer",
  "version": "1.0.2",
  "description": "Use JSDOM to connect on given URL with fake browser agent and return content of window variable",
  "main": "lib/pagesurfer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "grunt"
  },
  "author": "Gabriel Melillo",
  "license": "ISC",
  "dependencies": {
    "node-jsdom": "^3.1.5",
    "request": "^2.85.0"
  },
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-coffee": "^2.0.0"
  }
}
